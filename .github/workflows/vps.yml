name: VPS
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: |
        wget -O install.sh https://raw.githubusercontent.com/mhsanaei/3x-ui/master/install.sh
        sudo bash install.sh --no-input
    - uses: mxschmitt/action-tmate@v3
